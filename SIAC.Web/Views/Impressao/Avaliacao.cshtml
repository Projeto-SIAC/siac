@model ImpressaoAvaliacaoViewModel

@{
    ViewBag.Title = "Imprimir " + Model.Avaliacao.CodAvaliacao;
}

@section Scripts {
    <script>
        $(function () {
            $('.ui.checkbox').checkbox();
            $('.ui.accordion').accordion();
            $('.ui.modal').modal();

            $('.informacoes.button').click(function () {
                $('.informacoes.modal').modal('show');
            });
        });
    </script>
}

<h2>
    @ViewBag.Title
    <div class="ui right floated button informacoes">Informações</div>
</h2>

@Html.Partial("_AvaliacaoInformacoes", Model.Avaliacao)

<form class="ui form" method="post">
    <div class="field required">
        <label>Título</label>
        <input type="text" name="txtTitulo" placeholder="Título" value="@($"{Model.Avaliacao.TipoAvaliacao.Descricao} {Model.Avaliacao.CodAvaliacao}")" required />        
    </div>
    <div class="field required">
        <label>Instituição</label>
        <input type="text" name="txtInstituicao" placeholder="Título" value="@($"{Model.Avaliacao.Professor.Instituicao.PessoaJuridica.NomeFantasia}")" required />
    </div>
    <div class="field required">
        <label>Professor</label>
        <input type="text" name="txtProfessor" placeholder="Título" value="@($"{Model.Avaliacao.Professor.Usuario.PessoaFisica.Nome}")" required />
    </div>
    <div class="two fields">
        <div class="field">
            <label>Campos</label>
            <textarea name="txtCampos" placeholder="Campos">@Html.Raw("Nome\nData")</textarea>
        </div>
        <div class="field">
            <label>Instruções</label>
            <textarea name="txtInstrucoes" placeholder="Instruções"></textarea>
        </div>
    </div>
    <div class="field">
        <div class="ui toggle checkbox">
            <input type="checkbox" name="chkArquivar" checked>
            <label>Arquivar após imprimir</label>
        </div>
    </div>
    <div class="field">
        <input type="submit" class="ui button" value="Imprimir" />
    </div>
</form>

